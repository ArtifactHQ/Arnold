{
  "hooks": [
    {
      "event": "Stop",
      "command": "echo '💡 Reminder: If you just implemented a feature, run /spec-update to keep specification.md in sync.'",
      "description": "Remind to update spec after implementation"
    }
  ]
}
